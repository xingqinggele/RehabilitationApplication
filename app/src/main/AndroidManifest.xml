<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rehabilitationapplication">

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<!--        <activity android:name=".MainActivity"-->
<!--             android:configChanges="screenSize|keyboardHidden|orientation"/>-->
        <activity android:name=".activity.SportsIntroductionActivity"
             android:configChanges="screenSize|keyboardHidden|orientation"/>
        <activity android:name=".activity.GoSportsActivity"
             android:configChanges="screenSize|keyboardHidden|orientation"/>
        <activity android:name=".activity.HeartRateActivity" />
        <activity android:name=".activity.BloodOxygenActivity" />
        <activity android:name=".activity.BloodPressureActivity" />
        <activity android:name=".activity.KnowledgeDetailActivity" />
    </application>

</manifest>